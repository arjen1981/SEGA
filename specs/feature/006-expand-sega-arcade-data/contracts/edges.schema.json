{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "SEGA Graph Edges (006 — unchanged from 004)",
	"description": "Edge schema for 006-expand-sega-arcade-data. Identical to 004 schema with addition of existing 'partner of' label. No new edge types needed.",
	"type": "array",
	"items": {
		"type": "object",
		"required": ["from", "to", "label"],
		"properties": {
			"from": {
				"type": "string",
				"description": "Source node ID (must reference an existing node in nodes.json)",
				"pattern": "^[a-z0-9][a-z0-9-]*$"
			},
			"to": {
				"type": "string",
				"description": "Target node ID (must reference an existing node in nodes.json)",
				"pattern": "^[a-z0-9][a-z0-9-]*$"
			},
			"label": {
				"type": "string",
				"description": "Relationship type displayed on the edge",
				"enum": [
					"division of",
					"developed by",
					"runs on",
					"worked at",
					"directed",
					"produced",
					"designed",
					"programmed",
					"composed for",
					"artwork for",
					"partner of"
				]
			},
			"title": {
				"type": "string",
				"description": "Optional tooltip text (defaults to label if omitted)"
			}
		},
		"additionalProperties": false
	},
	"minItems": 1
}
